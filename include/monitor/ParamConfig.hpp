#pragma once
#include <cstddef>
#include <array>
#include <functional>
#include "envelope/envelope.hpp"

class ParamConfig
{
public:
    ParamConfig() = default;
    ~ParamConfig() = default;

private:
};